# TASKGENIUS Client Environment Variables
# Copy this file to .env and configure as needed

# Core API Base URL
# In development with Vite proxy, this can be empty (requests go to /api)
# In production, set to the full URL of core-api
VITE_CORE_API_BASE_URL=http://localhost:8000
